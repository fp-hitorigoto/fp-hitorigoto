name: 毎朝トレンド記事生成

on:
  # 毎朝8時（JST）に自動実行 = 23:00 UTC
  schedule:
    - cron: '0 23 * * *'
  # 手動でも実行できるようにする
  workflow_dispatch:

jobs:
  generate-trend-articles:
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

    permissions:
      contents: write

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Python 3.11 をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: scripts/requirements.txt

      - name: 依存パッケージをインストール
        run: pip install -r scripts/requirements.txt

      - name: トレンド記事を生成
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/fetch_trends_and_generate.py

      - name: 新しい記事をコミット・プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Actions"
          git add src/content/articles/ scripts/trends_processed_items.json || true
          git diff --staged --quiet || git commit -m "chore: トレンド記事生成 $(date '+%Y-%m-%d')"
          git push
